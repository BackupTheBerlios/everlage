# gets the agentID, password an isProviderAgent from the Agent table, where the name is a
# given UserAgent name
getUALogin=SELECT agentID, password, isProviderAgent FROM Agent WHERE name=?

# updates the Agent table for an given Agent
updateUA=UPDATE Agent SET addressRMI=?, agentSessionID=?, caSessionID=? WHERE agentID=?